<ul>
  <% @animals.each do |animal| %>
    <!--
      <li>
        <% link_to animal.name, animal_path(animal)  %></li>
      -->
    <div class="container">
      <div class="row">
        <div class="col-sm">
          Name: <%=animal.name %>
        </div>
        <div class="col-sm">
          State: <%=animal.state%>
        </div>
        <div class="col-sm">
          Category: <%=animal.category %>
        </div>
      </div>
  </div>
  <% end %>
</ul>
